@model IEnumerable<IndianRestaurant.Models.Menu>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout1.cshtml";
}

<h2>Index</h2>

<p>
    <a href="@Url.Action("Create")" class="btn ttable-header" title="Add"><i class="fa fa-plus"></i>Add New Item</a>
</p>
<table class="ttable">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td width="60%">
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td width="40%">
            <a href="@Url.Action("Edit", new { id = item.MenuId })" class="btn ttable-header" title="Edit"><i class="fa fa-pencil"></i></a>
            <a href="@Url.Action("Details", new { id = item.MenuId })" class="btn ttable-header" title="Info"><i class="fa fa-info"></i></a>
            <a href="@Url.Action("Delete", new { id = item.MenuId })" class="btn ttable-header" title="Delete"><i class="fa fa-trash"></i></a>

        </td>
    </tr>
}

</table>
